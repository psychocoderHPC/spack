# -------------------------------------------------------------------------
# This is the default spack repository configuration. It includes the
# builtin spack package repository.
#
# Users can override these settings by editing the following files.
#
# Per-spack-instance settings (overrides defaults):
#   $SPACK_ROOT/etc/spack/repos.yaml
#
# Per-user settings (overrides default and site settings):
#   ~/.spack/repos.yaml
# -------------------------------------------------------------------------
repos:
  - $spack/../../spack/repos/spec
  - $spack/../../benchspec/MPIACCEL/699.helloworld_s/spack
  - $spack/../../benchspec/MPIACCEL/620.dashmg_s/spack
  - $spack/var/spack/repos/builtin
